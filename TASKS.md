# TASKS — Leadgen LT

Легенда: ✅ готово · 🔄 в работе · 📋 запланировано
План из техспека §11. Агент для каждой задачи указан в скобках.

## Фаза 1 — личный инструмент

- ✅ **1. Инициализация проекта** (devops) — Next.js (App Router, TS), Prisma, Postgres, Docker Compose, CI/CD. Прошло Codex-гейт (3 прохода).
- ✅ **2. Схема БД (мультитенант)** (db-schema) — 8 моделей, 11 enum, init-миграция. `subscriber_id` везде, индексы. Прошло Codex-гейт.
- 📋 **3. Auth + форма реквизитов** (frontend-dev) — мой аккаунт, 2FA (email-код)/Google OAuth, обязательная форма реквизитов при регистрации (форма работы, реквизиты → профиль подписчика)
- ✅ **4. `rc-sync`** (backend-pipeline) — забор классификатора EVRK (карта UUID→kodas) + всех `veikiantis=1` по курсору, upsert в companies. Проверен на живом API (dry-run), прошёл Codex-гейт (3 прохода). См. `docs/API_RC.md`.
- ✅ **5. `enrich`** (backend-pipeline) — БОЕВОЙ воркер на rekvizitai по коду готов и **развёрнут на VPS** (2026-06-06). Строгий Tinklalapis-экстрактор + кредит-риск A..E + финансы из API реестра + домен-угадка (транслит LT) + email, 6 потоков, FETCH_ERR≠нет-сайта, D/E→archived_garbage. Прошёл Codex-гейт (2 находки исправлены). Самообогащение по cron `*/10` (~28k/день). Ночной 12k импортирован. Схема расширена миграцией. См. `AGENT_HANDOFF.md` (сессия 2026-06-06).
  - **2026-06-05:** найден и исправлен **баг website-парсера** — старый регэксп `svetainė\s+...` (плейн-текст) терял сайты, оформленные строкой таблицы `<td class="name">Tinklalapis</td><td class="value"><a href>` (показатель — LITIT/`litit.tech`). Правильный экстрактор = строгий по строке Tinklalapis. Идёт ре-скан 8 480 «без сайта» на VPS (`/tmp/resite.py`).
  - Решена **архитектура обогащения = 3 БЕСПЛАТНЫХ слоя**: rekvizitai по коду → домен-угадка `{имя}.lt/.com/.eu/.tech` → поиск по имени через поисковый API (Serper/Brave; бесплатный скрейп поисковиков с VPS не работает).
  - Платный XML-фид rekvizitai (€1150/год за 30k) **отклонён** — остаёмся на бесплатном скрейпе.
  - Версия на Places закоммичена; боевой воркер ещё не написан. См. `AGENT_HANDOFF.md`.
  - **2026-06-10:** обогащение стояло 2 дня — Cloudflare забанил **IPv4** VPS (rekvizitai за CF), а контейнер ходил только по IPv4 → 403. Фикс: rekvizitai-фетчи через `node:https` с `family=6` (строго IPv6, чистый), `enable_ipv6` в docker-сети. Потоки 10, cron `--limit=800`. Блоков 0, база добивается. ⚠️ IPv6 — последний чистый IP, не долбить вечно.
- ✅ **6. Дашборд + очередь review** (frontend-dev) — **переделан в КОКПИТ** (2026-06-10, PR #1): меню (Сводка/Требует проверки/База лидов), воронка (не обработано/в работе/ответили/игнор/отказ/сделки) + фильтр неделя/месяц/квартал, блок «кому позвонить», очередь `/review` (по `audit_status='done'`), `/leads` (таблица+фильтры), действия на карточке (взять в работу/исход/перезвонить/заметка/запустить аудит). Ярлык A: «Плохой сайт»→«Есть сайт» (нельзя утверждать «плохой» до аудита). Рабочий слой на `LeadDelivery` (+ next_call_at/last_contacted_at/note). v1 был на VPS; кокпит ждёт деплоя. Осталось: настоящий Auth/мультитенант (зад. 3/12).
- ✅ **7. `audit-gen`** (backend-pipeline) — **готов** (2026-06-10, PR #1): дренаж очереди `audit_status='queued'` → PageSpeed mobile+desktop → шаблонный `audit_issues` (LCP/TBT/FCP) → done/failed. Аудит НЕ авто — только по кнопке «Запустить аудит» в дашборде; cron `*/3` лишь дренажит очередь. LLM-вариант аудита — позже (Tier 3).
- 📋 **8. Отправка писем (мой домен webvibe.lt)** (backend-pipeline) — Resend + верификация домена сразу (прогрев репутации). ⚠️ нужен RESEND_API_KEY + верификация домена + GDPR-подтверждение.
- 📋 **9. Интеграция с CRM** (backend-pipeline) — API/webhooks: лид→CRM при отправке, статусы, won по оплате, обратные сигналы, статистика воронки
- ✅ **10. PWA + push** (frontend-dev + backend-pipeline) — **готов** (2026-06-10, PR #1): manifest+SW+иконки, установка на Домой, тоггл уведомлений, VAPID, модель `PushSubscription`, API `/api/push/*`. Триггер пуша = «аудит готов» (хук в audit-gen). ⚠️ на VPS нужны валидные VAPID_PUBLIC_KEY/PRIVATE_KEY.
- ✅ **11. `recheck`** (backend-pipeline) — **готов** (2026-06-10, PR #1): воркер расставляет компании по вёдрам `LeadState` (active_lead = лид A/B/C как в дашборде / dead = RC-неактивна или archived_garbage / recheck_later = прочее обогащённое), курсорный обход, cron `30 4 * * *`. Безопасно: пишет только `lead_state` + `enrichment.recheck_at` (+90д для recheck_later), `enrich_status` НЕ трогает. Ре-энрич по due recheck_at — TODO фазы 2.

> К демо с клиентом: пп. 1–2, 4–6 на одной нише (стройка/недвижимость). Обогащение в фоне, дашборд параллельно.
> Отправка/CRM/PWA/push/recheck — после, к демо не критичны.

## Фаза 2 — подписка

- 📋 **12. Активация мультитенанта** (db-schema + frontend-dev) — регистрация подписчиков (изоляция готова)
- 📋 **13. Онбординг + настройки** (frontend-dev + designer) — ниши, профиль, цены, Resend API + верификация домена
- 📋 **14. `lead-select`** (backend-pipeline) — дневной отбор по тарифу + дедупликация
- 📋 **15. `email-gen`** (backend-pipeline) — GPT-4o литовский (после замера себестоимости)
- 📋 **16. Очередь подтверждения + отправка** (frontend-dev + backend-pipeline) — через Resend подписчика
- 📋 **17. Биллинг + триал** (frontend-dev) — тарифы, триал 3 дня с картой

## Фаза 3+ — горизонт
- 📋 18. SMM/SEO/дизайн направления · 19. Маркетплейс «Paslaugos 2.0» · 20. CRM в Tier 4 (мультитенант SaaS)

## Открытые вопросы (замерить на данных)
1. Себестоимость аудита/письма (токены, % перегенераций литовского) — ДО фиксации цен Tier 2–3
2. Аудит: шаблон vs LLM — протестировать оба
3. % `ambiguous`/нерешённых по стройке/недвижимости — на прогоне enrich
4. Алгоритм `places_match_score` — признаки/веса/пороги, калибровать на реальных данных
5. Frontend хостинг: Vercel+VPS vs всё в Docker
6. Новый GitHub-репо vs текущий origin с чистой историей
